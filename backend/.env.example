APP_ENV=development
AUTH_MODE=development_header
DATABASE_URL=postgresql+psycopg://grantbridge:change-me@localhost:5432/grantbridge
LOG_LEVEL=INFO
API_V1_PREFIX=/api/v1
# Required only when AUTH_MODE=oidc. Provider selection and production values require approval.
# OIDC_ISSUER=https://identity.example.test/tenant/v2.0
# OIDC_AUDIENCE=grantbridge-api
# OIDC_TENANT_CLAIM=grantbridge_tenant_id
